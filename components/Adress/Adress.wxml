<!--components/Adress/Adress.wxml-->
<view class="consignee-info {{column > 1 ? 'small' : ''}}" wx:if="{{data}}">
  <text class="consignee-title">{{data.buyerName}} {{data.buyerMobile}}</text>
  <text class="page-text">{{data.buyerProvince}}{{data.buyerCity}}{{data.buyerAddress}}</text>
</view>
