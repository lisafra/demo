<!--components/Button/Button.wxml-->
<view class="page-footer-placeholder" wx:if="{{placeholderArea}}"></view>
<view class="page-footer">
  <button class="theme-button" type="primary" disabled="{{disabled}}">{{text}}</button>
  <slot></slot>
</view>
